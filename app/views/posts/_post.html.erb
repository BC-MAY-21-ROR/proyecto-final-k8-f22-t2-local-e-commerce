<div id="<%= dom_id post %>">
    <%= post.title%>
    <%= post.description%>
    <% post.picture.each do |file| %>
        <%= image_tag url_for(file) %>
    <% end %>
    <%= post.price%>
    <%= post.type_id==1 ? "Producto": "Servicio" %>
</div>
